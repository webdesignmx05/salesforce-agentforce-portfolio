{
  "name": "agentforce-backend-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Express proxy for Salesforce OAuth/API calls. Starts in mock mode so the portfolio deploys safely before real credentials are added.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "lint": "node --check src/server.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1"
  }
}
